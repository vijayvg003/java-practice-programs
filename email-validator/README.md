# Email Validator (Java)

This Java program validates an email address using Regular Expressions (Regex).

## Features
- Checks email format
- Uses regex pattern matching
- Console-based input validation

## Technologies Used
- Java
- Regex (Regular Expressions)
- String methods
